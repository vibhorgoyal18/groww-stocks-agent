# ═══════════════════════════════════════════════════════════════════
# AI Stock Trading Agent - Environment Configuration
# ═══════════════════════════════════════════════════════════════════
# This is a sample configuration file. Copy this to .env and fill in your values.
# Command: cp .env.sample .env

# ───────────────────────────────────────────────────────────────────
# 🤖 AI Configuration
# ───────────────────────────────────────────────────────────────────
# OpenAI API key for GPT-4 powered trading decisions and analysis
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ───────────────────────────────────────────────────────────────────
# 📈 Groww API Configuration
# ───────────────────────────────────────────────────────────────────
# Choose your authentication method: 'token' or 'totp'
# 'token' = OAuth 2.0 Token (requires daily approval from Groww)
# 'totp' = TOTP Flow (no expiry, more permanent, recommended)
GROWW_AUTH_METHOD=token

# ─── Option 1: OAuth 2.0 Token Method (Requires daily approval) ───
# Generate from: https://groww.in/trade-api/api-keys
# Click 'Generate API key' and copy the token
GROWW_API_TOKEN=your_groww_api_token_here

# ─── Option 2: TOTP Method (No expiry, recommended) ───
# Generate from: https://groww.in/trade-api/api-keys
# Click dropdown next to 'Generate API key' → 'Generate TOTP token'
# Copy both the TOTP Token (API Key) and TOTP Secret
# You can also scan the QR code with an authenticator app
GROWW_TOTP_TOKEN=your_totp_api_key_here
GROWW_TOTP_SECRET=your_totp_secret_here

# ───────────────────────────────────────────────────────────────────
# 💰 Trading Strategy Configuration
# ───────────────────────────────────────────────────────────────────

# Maximum investment amount per trading session (in INR)
# This limits how much capital the agent can deploy in a single execution
# Default: 10000 (₹10,000)
MAX_INVESTMENT_AMOUNT=10000

# Risk tolerance threshold (as decimal: 0.20 = 20%)
# Higher values allow riskier trades with potentially higher returns
# Lower values focus on safer, more conservative positions
# Range: 0.05 (5%) to 0.30 (30%)
# Default: 0.20 (20% risk tolerance)
RISK_THRESHOLD=0.20

# Minimum expected return target (as percentage)
# Only consider stocks that meet or exceed this return target
# This is the target return for the time horizon specified below
# Default: 15.0 (15% expected return)
MIN_EXPECTED_RETURN=15.0

# Expected return time horizon (in days)
# The number of days in which the MIN_EXPECTED_RETURN should be achieved
# Shorter timeframes = more aggressive trading
# Longer timeframes = more patient, strategic positions
# Default: 30 (30 days / ~1 month)
EXPECTED_RETURN_DAYS=30

# ───────────────────────────────────────────────────────────────────
# 📝 System Configuration
# ───────────────────────────────────────────────────────────────────

# Logging level for application logs
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG: Most verbose, all details
# INFO: General information about operations (recommended)
# WARNING: Only warnings and errors
# ERROR: Only errors
# Default: INFO
LOG_LEVEL=INFO

# ═══════════════════════════════════════════════════════════════════
# END OF CONFIGURATION
# ═══════════════════════════════════════════════════════════════════
